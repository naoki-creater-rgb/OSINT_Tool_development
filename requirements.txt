# Web / API
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0

# Graph / Data Structure
networkx>=3.2.1

# Asynchronous HTTP Client
# ghunt が httpx<0.28 を要求するため上限を固定（ddgs は実通信に primp を使うので 0.27 系で動作する）
httpx>=0.27.0,<0.28.0

# Search (googlesearch_client): DuckDuckGo。APIキー不要
ddgs>=9.0.0

# Configuration
python-dotenv>=1.0.1

# Testing (Development)
pytest>=8.0.0
pytest-asyncio>=0.23.5